# Visual Architecture & Flow Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     MoodTunes System                        │
└─────────────────────────────────────────────────────────────┘

┌────────────────────────┐              ┌────────────────────────┐
│   FRONTEND (React)     │              │   BACKEND (Flask)      │
│  http://localhost:3000 │              │  http://localhost:5000 │
├────────────────────────┤              ├────────────────────────┤
│                        │              │                        │
│  HomePage              │              │  app.py                │
│  ├─ Setup Btn──────────┼──────────────┼─→ Routes Setup        │
│  └─ Features           │              │                        │
│                        │              │  routes.py             │
│  SetupPage             │              │  ├─ /api/detect      │
│  ├─ Language Select    │              │  ├─ /api/history     │
│  ├─ Region Select      │  HTTP/JSON   │  ├─ /api/preferences │
│  ├─ History Display    │◄─────────────┤─ /api/records        │
│  └─ Start Btn──────────┼──────────────┤─ /api/user/*         │
│     │                  │              │                        │
│     └──────────────────┼──────────────┼─→ emotion.py          │
│                        │              │   (Detection Model)    │
│  ExperiencePage        │              │                        │
│  ├─ Camera             │              │  recommendations.py    │
│  ├─ Preferences        │  Image +     │  (Music Logic)         │
│  ├─ Detect Btn         │  Metadata    │                        │
│  └─ Recommendations    │◄─────────────┤─ mongo_db.py          │
│                        │              │  (DB Operations)       │
│  AuthContext           │              │                        │
│  └─ User Management    │              │  config.py             │
│                        │              │  (Settings)            │
└────────────────────────┘              └────────────────────────┘
         │                                        │
         │                                        │
         └────────────────┬─────────────────────┘
                          │
              ┌───────────▼────────────┐
              │   MongoDB Database     │
              │ http://localhost:27017 │
              ├───────────────────────┤
              │                       │
              │ Collections:          │
              │ ├─ mood_records       │
              │ │  └─ Emotion data    │
              │ │                     │
              │ └─ user_preferences   │
              │    └─ User settings   │
              └───────────────────────┘
```

---

## User Flow Diagram

```
┌──────────────────┐
│    User Opens    │
│   Application    │
│  localhost:3000  │
└────────┬─────────┘
         │
         ▼
    ┌────────────────────┐
    │   HomePage.jsx     │
    ├────────────────────┤
    │ • Hero Section     │
    │ • Features List    │
    │ • Get Started Btn  │
    └────────┬───────────┘
             │ onClick: goToSetup()
             ▼
    ┌────────────────────────────────┐
    │   SetupPage.jsx (/setup)       │
    ├────────────────────────────────┤
    │ Left Panel:                    │
    │ • Language Selector (6 opt)    │
    │ • Region Selector (6 opt)      │
    │ • Start Detection Btn          │
    │                                │
    │ Right Panel:                   │
    │ • Your History Header          │
    │ • Loading State                │
    │ • History Items:               │
    │   - Emotion Emoji              │
    │   - Emotion Name               │
    │   - Confidence %               │
    │   - Date & Region              │
    └────────┬─────────────────────────┘
             │ onClick: handleStart()
             │ OR onClick: handleHistoryClick()
             ▼
    ┌────────────────────────────────┐
    │  ExperiencePage.jsx (/exp)     │
    ├────────────────────────────────┤
    │ Left Panel:                    │
    │ • Language Selector            │
    │ • Region Selector              │
    │                                │
    │ Right Panel:                   │
    │ • Camera Preview               │
    │ • Detect Btn                   │
    │                                │
    │ Bottom Section:                │
    │ • Emotion Result Box           │
    │   - Large Emoji                │
    │   - Emotion Name               │
    │   - Confidence Score           │
    │                                │
    │ • Recommendations Section:     │
    │   - 10 Song Cards             │
    │   - Song Details              │
    │   - Spotify Link              │
    │                                │
    │ • How It Works Guide           │
    └────────┬─────────────────────────┘
             │ Back to Setup
             └─────────────────────┐
                                   │
                      ┌────────────▼──────┐
                      │ History Updated   │
                      │ in MongoDB        │
                      │                   │
                      │ Can click item    │
                      │ to revisit        │
                      └──────────────────┘
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    Emotion Detection Flow                  │
└─────────────────────────────────────────────────────────────┘

Step 1: Image Capture
┌──────────────────────────┐
│  User enables camera     │
│  in ExperiencePage       │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  User positions face     │
│  in camera frame         │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  Click "Detect Emotion"  │
│  button                  │
└──────────┬───────────────┘

Step 2: Image Processing
           │
           ▼
┌──────────────────────────────────────┐
│  Frontend captures image             │
│  Converts to Base64                  │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Frontend sends POST to              │
│  /api/detect with:                   │
│  • image_data: "base64..."           │
│  • metadata: {email, name, ...}      │
│  • limit: 10                         │
└──────────┬──────────────────────────┘

Step 3: Backend Processing
           │
           ▼
┌──────────────────────────────────────┐
│  Backend receives request            │
│  Decodes Base64 image                │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  emotion.py processes image:         │
│  • Face detection (OpenCV)           │
│  • Image preprocessing               │
│  • Model inference (TensorFlow)      │
│  • Returns: emotion label            │
│           confidence score           │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  recommendations.py generates:       │
│  • 10 song recommendations           │
│  • Based on emotion                  │
│  • Spotify audio features            │
└──────────┬──────────────────────────┘

Step 4: Database Storage
           │
           ▼
┌──────────────────────────────────────┐
│  mongo_db.py saves record:           │
│  • emotion detected                  │
│  • confidence score                  │
│  • user metadata                     │
│  • language & region                 │
│  • track recommendations             │
│  • timestamp                         │
│  • (optional: image data)            │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  MongoDB mood_records collection:    │
│  Document stored successfully        │
└──────────┬──────────────────────────┘

Step 5: Response & Display
           │
           ▼
┌──────────────────────────────────────┐
│  Backend returns response:           │
│  {                                   │
│    "emotion": "happy",               │
│    "confidence": 0.95,               │
│    "tracks": [...]                   │
│  }                                   │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Frontend displays:                  │
│  • Large emotion emoji               │
│  • Emotion name                      │
│  • Confidence percentage              │
│  • 10 song recommendation cards      │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  User sees music recommendations     │
│  Can click to listen on Spotify      │
│  (when Spotify API connected)        │
└──────────────────────────────────────┘
```

---

## History Tracking Flow

```
┌─────────────────────────────────────────────────────────────┐
│              History Tracking & Retrieval                   │
└─────────────────────────────────────────────────────────────┘

On SetupPage Load:
┌──────────────────────────────────────────┐
│  User navigates to /setup                │
│  useEffect triggered                     │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Check if user logged in                 │
│  Get user email from AuthContext         │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Fetch GET /api/history?email=xxx        │
│  with limit=10                           │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Backend queries MongoDB:                │
│  db.mood_records.find({email: xxx})      │
│  .sort({created_at: -1})                 │
│  .limit(10)                              │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Convert ObjectIds to strings            │
│  Return array of records                 │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Frontend receives records               │
│  setHistory(records)                     │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Render history list:                    │
│  For each record:                        │
│  • Get emotion emoji                     │
│  • Show emotion name                     │
│  • Show confidence %                     │
│  • Show region                           │
│  • Show date/time                        │
│  • Make clickable                        │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  User sees history on SetupPage          │
│  With all past emotions recorded         │
└──────────────────────────────────────────┘

On History Item Click:
           │
           ▼
┌──────────────────────────────────────────┐
│  handleHistoryClick(record)              │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Navigate to /experience with state:     │
│  {                                       │
│    language: record.language,            │
│    region: record.region,                │
│    previousRecord: record                │
│  }                                       │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  ExperiencePage loads with:              │
│  • Pre-filled language                   │
│  • Pre-filled region                     │
│  • Previous emotion shown                │
│  • Previous recommendations shown        │
└──────────────────────────────────────────┘
```

---

## Database Schema Visualization

```
MongoDB Collections Structure:

┌─────────────────────────────────────────────────────┐
│          mood_records Collection                    │
├─────────────────────────────────────────────────────┤
│ Document Example:                                   │
│ {                                                   │
│   "_id": ObjectId("..."),                           │
│   "email": "user@example.com",                      │
│   "name": "John Doe",                               │
│   "region": "IN",                                   │
│   "language": "en",                                 │
│   "detected_emotion": "happy",                      │
│   "confidence": 0.9523,                             │
│   "spotify_tracks": [                               │
│     {                                               │
│       "name": "Happy Song",                         │
│       "artists": ["Artist 1", "Artist 2"],          │
│       "album": "Album Name",                        │
│       "external_urls": {                            │
│         "spotify": "https://..."                    │
│       }                                             │
│     },                                              │
│     ... (10 tracks total)                           │
│   ],                                                │
│   "image_data": "base64_encoded_string",            │
│   "created_at": ISODate("2025-12-03T10:30:00Z")    │
│ }                                                   │
│                                                     │
│ Indexes:                                            │
│ • email (for user lookups)                          │
│ • created_at (for sorting)                          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│       user_preferences Collection                   │
├─────────────────────────────────────────────────────┤
│ Document Example:                                   │
│ {                                                   │
│   "_id": ObjectId("..."),                           │
│   "email": "user@example.com" (unique),             │
│   "name": "John Doe",                               │
│   "preferred_language": "en",                       │
│   "preferred_region": "IN",                         │
│   "spotify_token": "BQD...",                        │
│   "updated_at": ISODate("2025-12-03T10:30:00Z")    │
│ }                                                   │
│                                                     │
│ Indexes:                                            │
│ • email (unique)                                    │
└─────────────────────────────────────────────────────┘
```

---

## API Endpoint Summary

```
┌──────────────────────────────────────────────────────────┐
│             API Endpoints Overview                       │
└──────────────────────────────────────────────────────────┘

1. POST /api/detect
   ├─ Purpose: Detect emotion from image
   ├─ Request: Base64 image + metadata
   ├─ Response: { emotion, confidence, tracks }
   └─ Side Effects: Saves to MongoDB

2. GET /api/history
   ├─ Purpose: Get user's emotion history
   ├─ Query: email, limit
   ├─ Response: { records: [...], total: count }
   └─ Database: Queries mood_records

3. GET /api/history/<record_id>
   ├─ Purpose: Get specific emotion record
   ├─ Path: record ID
   ├─ Response: Single record object
   └─ Database: Finds by ObjectId

4. GET /api/user/preferences
   ├─ Purpose: Get user preferences
   ├─ Query: email
   ├─ Response: User preferences object
   └─ Database: Queries user_preferences

5. POST /api/user/preferences
   ├─ Purpose: Create/update user preferences
   ├─ Body: { email, name, language, region }
   ├─ Response: { message: "Updated" }
   └─ Database: Upserts user_preferences

6. GET /api/records (Legacy)
   ├─ Purpose: Get all/filtered records
   ├─ Query: email, limit
   ├─ Response: Array of records
   └─ Database: Queries mood_records
```

---

## Component Hierarchy

```
┌──────────────────────────────────────┐
│           App.jsx                    │
│      (React Router Setup)            │
└──────────────┬───────────────────────┘
               │
      ┌────────┴────────┬────────────┐
      │                 │            │
      ▼                 ▼            ▼
  HomePage.jsx    SetupPage.jsx   ExperiencePage.jsx
      │                 │            │
      │                 ▼            ▼
      │          ┌────────────┐  ┌──────────────┐
      │          │Preferences │  │EmotionDetector
      │          │   Panel    │  │  Component   │
      │          ├────────────┤  └──────────────┘
      │          │  History   │
      │          │  Section   │
      │          └────────────┘
      │
      └─────────────────┬───────────────────┐
                        │                   │
                        ▼                   ▼
                  (Navigation)          (API Calls)
                        │                   │
                        │                   ▼
                  ┌──────────────┐   ┌────────────────┐
                  │AuthContext   │   │Fetch to Backend│
                  │  Provider    │   │    Routes      │
                  └──────────────┘   └────────────────┘
```

---

## Technology Stack

```
┌─────────────────────────────────────────────────────────┐
│              Technology Stack                           │
└─────────────────────────────────────────────────────────┘

FRONTEND:
├─ React 18+ (UI Library)
├─ React Router v6 (Routing)
├─ CSS3 (Styling)
│  ├─ Flexbox
│  ├─ CSS Grid
│  └─ Gradients & Animations
├─ JavaScript ES6+ (Logic)
└─ Local Storage (Session Data)

BACKEND:
├─ Flask (Web Framework)
├─ Python 3.8+ (Runtime)
├─ pymongo (MongoDB Driver)
├─ OpenCV (Face Detection)
├─ TensorFlow/Keras (AI Model)
├─ NumPy (Array Processing)
└─ python-dotenv (Configuration)

DATABASE:
├─ MongoDB 4.4+
├─ Collections (mood_records, user_preferences)
├─ Indexes (email, created_at)
└─ BSON (Document Format)

OPTIONAL (For Enhancement):
├─ Spotify Web API
├─ Firebase Authentication
├─ AWS/GCP/Azure (Deployment)
└─ Heroku/Vercel (CI/CD)
```

---

This visual guide shows the complete architecture and flow of your MoodTunes application! 🎵
